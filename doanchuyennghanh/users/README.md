# Users Web App

Ứng dụng web người dùng được xây dựng với Next.js (App Router), TypeScript và Tailwind CSS. Mục tiêu là cung cấp giao diện hiện đại, dễ mở rộng, dễ bảo trì, kèm theo các tiện ích UI.

## 🚀 Tính Năng Chính

- Giao diện hiện đại với Tailwind CSS.
- App Router (`app/`) của Next.js cho điều hướng mượt mà.
- Cấu trúc module hóa trong `features/` (navbar, footer, layouts, home).
- Tối ưu tải font và asset với Next.js.
- Hỗ trợ TypeScript và ESLint cấu hình sẵn.

## 🛠️ Công Nghệ Sử Dụng

- Next.js 14+ (App Router)
- React 18
- TypeScript
- Tailwind CSS
- ESLint
- PostCSS
- shadcn/ui (cấu hình qua `components.json`, tùy chỉnh UI)

## 📦 Yêu Cầu Hệ Thống

- Node.js >= 18
- npm hoặc yarn hoặc pnpm hoặc bun

## ▶️ Cài Đặt & Chạy

Cài đặt dependencies:
```bash
npm install
```

Chạy ở môi trường phát triển:
```bash
npm run dev
```

Build sản phẩm:
```bash
npm run build
```

Chạy sản phẩm đã build:
```bash
npm run start
```

Lint code:
```bash
npm run lint
```

Mặc định ứng dụng chạy tại: http://localhost:3000

## 📁 Cấu Trúc Thư Mục
